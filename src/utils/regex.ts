/**
 * @file src/utils/regex.ts
 * @description Regular expressions used throughout the application.
 */


/**
 * Contains a collection of regex patterns used throughout the application.
 */
export const RegexPatterns = {
    ensureSupportedAuthProtocol: /^(oidc|oauth2|saml)$/
}
